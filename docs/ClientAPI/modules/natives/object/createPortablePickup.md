# createPortablePickup (Category object)

```js
function createPortablePickup(pickupHash: number, x: number, y: number, z: number, placeOnGround: boolean, modelHash: number): number
```

[filename](createPortablePickup_m.md ':include')